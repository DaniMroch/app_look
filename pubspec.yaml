name: app_look
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  cloud_firestore: ^4.17.5
  device_preview: ^1.1.0
  firebase_auth: ^4.20.0
  firebase_cloud_firestore: ^4.15.8
  firebase_core: ^2.32.0
  flutter:
    sdk: flutter
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/irritado.png
    - assets/alegre.png
    - assets/triste.png
    - assets/calmo.png
    - assets/calmo_frio.png
